<Window x:Class="IsparkDownloader2.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="发现新版本" 
        Height="480" Width="560"
        WindowStartupLocation="CenterOwner"
        Background="#FF1E1E1E"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF3C3C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="600"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF505050"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF2A2A2A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FF00D4AA"/>
            <Setter Property="Foreground" Value="#FF0F0F1A"/>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#FF3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="&#x1F680;" FontSize="28" Margin="0,0,12,0" VerticalAlignment="Center"/>
            <StackPanel>
                <TextBlock Text="发现新版本" Foreground="White" FontSize="20" FontWeight="Bold"/>
                <TextBlock x:Name="VersionText" Text="最新版本: v1.0.0" Foreground="#FF00D4AA" FontSize="14" Margin="0,2,0,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Current vs New -->
        <Border Grid.Row="1" Background="#FF252525" CornerRadius="8" Margin="0,0,0,16" Padding="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="16,12">
                    <TextBlock Text="当前版本" Foreground="#FF8B8BA7" FontSize="11" Margin="0,0,0,4"/>
                    <TextBlock x:Name="CurrentVersionText" Text="v1.0.0" Foreground="White" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="&#x2192;" Foreground="#FF00D4AA" FontSize="20" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="2" Margin="16,12">
                    <TextBlock Text="最新版本" Foreground="#FF8B8BA7" FontSize="11" Margin="0,0,0,4"/>
                    <TextBlock x:Name="NewVersionText" Text="v1.1.0" Foreground="#FF00D4AA" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Release Info -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="发布日期" Foreground="#FF8B8BA7" FontSize="11" Margin="0,0,0,2"/>
            <TextBlock x:Name="ReleaseDateText" Text="2026-01-01" Foreground="White" FontSize="13" Margin="0,0,0,10"/>
            <TextBlock Text="更新内容" Foreground="#FF8B8BA7" FontSize="11" Margin="0,0,0,4"/>
        </StackPanel>

        <!-- Release Notes -->
        <Border Grid.Row="3" Background="#FF252525" CornerRadius="8" Margin="0,0,0,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="12">
                <TextBlock x:Name="ReleaseNotesText" 
                           Text="- 修复了一些已知问题&#x0a;- 优化了下载性能"
                           Foreground="#FFCCCCCC" FontSize="12" TextWrapping="Wrap"
                           LineHeight="20"/>
            </ScrollViewer>
        </Border>

        <!-- Progress -->
        <Grid Grid.Row="4" x:Name="ProgressPanel" Visibility="Collapsed" Margin="0,0,0,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ProgressBar x:Name="UpdateProgressBar" Grid.Row="0" 
                         Minimum="0" Maximum="1" Value="0"
                         Height="6" Background="#FF2A2A4A" Foreground="#FF00D4AA"
                         BorderThickness="0"/>
            <TextBlock x:Name="ProgressText" Grid.Row="1" 
                       Text="正在下载更新... 0%" Foreground="#FF8B8BA7" FontSize="11"
                       HorizontalAlignment="Center" Margin="0,6,0,0"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="UpdateNowButton" Content="立即更新" Style="{StaticResource PrimaryButton}" Click="UpdateNow_Click"/>
            <Button x:Name="LaterButton" Content="稍后提醒" Style="{StaticResource SecondaryButton}" Click="Later_Click"/>
            <Button x:Name="SkipButton" Content="跳过此版本" Style="{StaticResource SecondaryButton}" Click="Skip_Click"/>
        </StackPanel>
    </Grid>
</Window>
