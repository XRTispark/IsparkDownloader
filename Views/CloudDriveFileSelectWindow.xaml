<Window x:Class="IsparkDownloader2.Views.CloudDriveFileSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="选择要下载的文件"
        Height="550" Width="650"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        FontFamily="{StaticResource FontFamilyBase}"
        FontSize="13">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="网盘分享文件列表" Foreground="{StaticResource TextPrimaryBrush}" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="保存路径:" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" x:Name="SavePathTextBox" Style="{StaticResource ModernTextBox}"/>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource BgElevatedBrush}" CornerRadius="6">
            <ListView x:Name="FileListView" Background="Transparent" BorderThickness="0"
                      SelectionMode="Extended"
                      Style="{StaticResource ModernListView}"
                      ItemContainerStyle="{StaticResource ModernListViewItem}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="文件名" Width="300">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding FileName}" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                        <TextBlock Text=" [文件夹]" Foreground="{StaticResource AccentBrush}" FontSize="10"
                                                   Visibility="{Binding IsFolder, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="大小" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FileSize, StringFormat={}{0:F2} MB}"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Visibility="{Binding IsFolder, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="修改时间" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ModifyTime, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <TextBlock Grid.Row="3" x:Name="StatusText" Text="请选择要下载的文件" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,8"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="全选" Style="{StaticResource BaseButton}" Click="SelectAll_Click"/>
            <Button Content="下载选中" Style="{StaticResource PrimaryButton}" Click="DownloadSelected_Click"/>
            <Button Content="取消" Style="{StaticResource BaseButton}" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
